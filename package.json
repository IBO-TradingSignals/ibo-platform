{
  "name": "ibo-platform",
  "version": "0.1.0",
  "private": true,
  "description": "IBO Platform monorepo — subscription-based trading signals ecosystem (Backend is the Single Source of Truth).",
  "packageManager": "pnpm@12.4.1",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.10.0",
    "prettier": "^3.6.2",
    "turbo": "^2.10.12",
    "typescript": "~5.9.3",
    "typescript-eslint": "^8.70.0"
  }
}
