{
  "name": "@ibo/backend",
  "version": "0.1.0",
  "private": true,
  "description": "IBO Platform backend API — Single Source of Truth for business logic.",
  "scripts": {
    "build": "nest build",
    "lint": "eslint \"src/**/*.ts\"",
    "test": "vitest run",
    "start:dev": "nest start --watch",
    "start:prod": "node dist/main.js"
  },
  "dependencies": {
    "@nestjs/common": "^12.0.1",
    "@nestjs/core": "^12.0.1",
    "@nestjs/platform-express": "^12.0.1",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "@nestjs/cli": "^12.0.0",
    "@types/node": "^22.20.2",
    "typescript": "~5.9.3",
    "vitest": "^5.0.0"
  }
}
