# @ibo/contracts

Single Source of Truth for the IBO Platform API contract.

Phase 1: contains a minimal OpenAPI 3.1 spec (`openapi.yaml`) covering only the `GET /health` endpoint.

Phase 2+: full API contracts, schema validation, and generated types/SDKs for all clients.
